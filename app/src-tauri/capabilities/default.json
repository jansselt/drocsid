{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": ["main", "voice-popout"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "notification:default",
    "updater:default",
    "process:default"
  ]
}
